name: rxfi-benchmark
channels:
  - conda-forge
dependencies:
  - python=3.11.16
  - pip=26.2.1
  - pip:
      # CUDA-enabled wheels and their pinned CUDA 12.4 runtime dependencies.
      - --extra-index-url https://download.pytorch.org/whl/cu124
      - matplotlib==3.11.2
      - numcodecs==0.15.1
      - numpy==2.4.6
      - PyYAML==6.0.3
      - scikit-learn==1.9.1
      - timm==1.0.29
      - torch==2.5.1+cu124
      - torchvision==0.20.1+cu124
      - zarr==2.18.7
